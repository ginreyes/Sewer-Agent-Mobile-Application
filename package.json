{
  "name": "concertina-device-app",
  "version": "1.1.0",
  "description": "Concertina Device app — sends accurate device info and uploads video to Concertina",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "setup": "npm install && npx cap add android && npm run build && npx cap sync android",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:run": "npx cap run android",
    "check:permissions": "node scripts/check-permissions.js",
    "android:build": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:build:win": "npm run build && npx cap sync android && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run build && npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "capacitor": {
    "appId": "ai.concertina.device",
    "appName": "Concertina Device",
    "webDir": "dist"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/camera": "^6.1.3",
    "@capacitor/core": "^6.0.0",
    "@capacitor/device": "^6.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0",
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
